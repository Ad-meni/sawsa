import React from "react";
import ReactDOM from "react-dom";
import App from "./App"; // Importa el archivo que acabamos de crear

ReactDOM.render(<App />, document.getElementById("root"));
// Esto monta tu aplicación en el contenedor con id "root" dentro de tu archivo HTML